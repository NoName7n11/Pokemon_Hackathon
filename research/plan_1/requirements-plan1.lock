# Phases 0-7 lock: no third-party packages.
# The supervised bootstrap uses a deterministic standard-library hashed linear
# policy-value model. A later neural-backend spike must replace this file with a
# hash-locked dependency set before introducing NumPy, PyTorch, or ONNX Runtime.
