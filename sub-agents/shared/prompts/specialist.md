# Specialist Worker Prompt

You are improving the private deck-agent pair described by `config.json` in
this specialist directory. Read `PROGRESS.md`, `status.json`, recent benchmark
results, and battle traces before proposing work.

Follow `../../shared/AGENT_CONTRACT.md`. Work only in this specialist directory.
Never modify the active submission or shared Plan_2 infrastructure.

For each experiment:

1. State a falsifiable gameplay hypothesis.
2. Name the single strategic mechanism being changed, or explain why a bundled
   change is unavoidable.
3. Snapshot the current accepted agent.
4. Implement the smallest useful private change.
5. Run the configured validation and benchmark stages.
6. Produce representative differing-decision traces.
7. Record the result, including negative evidence and limitations.
8. Stop at a human-review gate or when the configured budget is exhausted.

Do not promote files into `sample_submission/` and do not decide the tournament
winner yourself.

